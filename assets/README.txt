This folder is reserved for future images/media (logos, screenshots, custom icons, etc.).
Currently all icons are inline SVG symbols embedded directly in index.html for full portability
(so the guide works standalone without a server) — see README.md for details.
